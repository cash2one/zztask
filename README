ZZ91任务管理系统
用于控制自定义任务的调度，监控等

zz91-mission-cron:提供给ZZ91用于执行各类任务
zz91-mission-local:提供给本地CRM及数据导入等使用
zz91-mission-jx:提供给环保网使

分支说明：
master
实为mission分支，与最新的release_board对应

release_board
最新的board分支

release_board_2.3.x
2.3版本的board分支，该版本不能支持多台服务器协作调度任务，并且未使用最新的zz91-utils工具包（老版本zz91-utils包含旧版solr接口），某些服务器仍然使用该版本服务，所以保留该分支

release_mission_3.x
对应release_board_2.3.x的任务，多为旧版任务
